export default function Loading() {
  return (
    <div className="flex min-h-screen items-center justify-center bg-ink-900 text-ink-50">
      <p className="font-mono text-xs uppercase tracking-[0.22em] text-neon-400">Hydrating engineering profile...</p>
    </div>
  );
}
